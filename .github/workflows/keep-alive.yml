name: Keep Alive
on:
  schedule:
    - cron: '*/13 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          curl -sf https://plus-sqxw.onrender.com/health || curl -sf https://plus-sqxw.onrender.com/plans
